/**
 * Booking status badge: the shared brand StatusPill (colored text on dark surfaces, dark text
 * with a colored dot/border inside .light-surface). Kept as an alias so existing imports work.
 */
export { STATUS_LABELS, StatusPill as StatusBadge } from "@/components/ui/status-pill";
