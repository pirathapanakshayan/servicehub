# Neon PostgreSQL connection string (use the direct, non-pooled URL for migrations)
DATABASE_URL="postgresql://neondb_owner:npg_s3HefY2cvFPV@ep-billowing-snow-b3dofh0l.c-4.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require"

# Separate, disposable database for automated tests. It is DROPPED and re-seeded on every
# test run, so never point it at DATABASE_URL (the test setup refuses if they match).
TEST_DATABASE_URL="postgresql://USER:PASSWORD@HOST.neon.tech/servicehub_test?sslmode=require"

# Secret used by jose to sign session JWTs. Generate with:
#   node -e "console.log(require('crypto').randomBytes(48).toString('base64url'))"
# Must be at least 32 characters.
JWT_SECRET="replace-with-a-long-random-string-of-at-least-32-chars"

NEXT_PUBLIC_APP_URL="http://localhost:3000"
